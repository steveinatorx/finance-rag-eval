# OpenAI API Key (optional - for enhanced embeddings and LLM generation)
# Get your key from: https://platform.openai.com/api-keys
# Note: Use organization-level key (starts with sk-) or project-scoped key (starts with sk-proj-)
export OPENAI_API_KEY=sk-your-key-here

# Optional: Project ID (only needed if using project-scoped key and want to specify project)
# export OPENAI_PROJECT_ID=proj-abc123...

# Optional: Use OpenAI embeddings instead of local sentence-transformers
# export EMBEDDING_MODEL=openai

# Optional: LLM model for answer generation
# export LLM_MODEL=gpt-3.5-turbo
# export LLM_TEMPERATURE=0.0

# Python path for development
export PYTHONPATH=src
